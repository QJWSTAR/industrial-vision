"""UI panels — composable UI components for the main window.

Note: HeaderPanel, LeftPanel, and RightPanel were removed as dead code.
MainWindow implements its own _app_header, _left_column, and _right_column
methods directly. These were never integrated into the active UI.
"""
