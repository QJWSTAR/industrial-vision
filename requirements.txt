# Minimal runtime dependencies for cold spray defect repair software.
# For full-featured install (loguru): pip install -e ".[full]"
# For development: pip install -e ".[dev]"
numpy>=1.21
scipy>=1.7
PySide6>=6.4
matplotlib>=3.5
shapely>=1.8
pyzmq>=24
protobuf>=5.26  # >=5.26 required: repair_protocol_pb2.py uses runtime_version (gencode v6.31.1)
reportlab>=4.0  # PDF 报告生成 (report_generator.py)
cryptography>=41.0  # RSA 许可证验证 (license_manager.py)