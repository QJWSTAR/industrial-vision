# 运维手册

> 版本：1.0.0 ｜ 更新日期：2026-07-10 ｜ 文档语言：简体中文

本文档面向 CSAM 冷喷涂缺陷修复软件的运维人员，系统说明日志管理、备份与恢复、升级流程、MATLAB 算法的新增与替换、发布流程、环境变量参考、日常运维检查清单及性能调优。配套阅读 [06 故障排查](06_故障排查.md) 与 [07 开发者文档](07_开发者文档.md)。

---

## 目录

- [日志](#日志)
- [备份](#备份)
- [恢复](#恢复)
- [升级](#升级)
- [新增 MATLAB 算法](#新增-matlab-算法)
- [替换算法](#替换算法)
- [发布流程](#发布流程)
- [环境变量运维参考](#环境变量运维参考)
- [日常运维检查清单](#日常运维检查清单)
- [性能调优](#性能调优)

---

## 日志

系统日志统一由 Python 应用日志管理，均支持级别控制与文件滚动。

### Python 日志

由 `repair_app/utils/logger_config.py` 配置，输出到 `logs/` 目录。

| 日志文件 | 级别 | 滚动策略 | 保留期 |
|----------|------|----------|--------|
| `logs/app_{date}.log` | 全部级别（DEBUG 及以上） | 单文件 10 MB 滚动 | 30 天 |
| `logs/error_{date}.log` | ERROR 及以上 | 按日期滚动 | 60 天 |
| `logs/structured_{date}.jsonl` | 全部级别（JSONL 结构化，可选） | 按日期滚动 | 30 天 |
| 控制台 | 全部级别 | — | — | 彩色输出 |

**日志级别**：`DEBUG` / `INFO` / `WARNING` / `ERROR`

**级别配置**：

```powershell
# Windows PowerShell
$env:CSAM_LOG_LEVEL = "DEBUG"

# macOS/Linux
export CSAM_LOG_LEVEL=DEBUG
```

生产环境默认 `INFO`，排查问题时切换为 `DEBUG`。

### MatlabAdapter 日志

MatlabAdapter 运行于 MATLAB 宿主进程内（通过 `pyenv` 调用 Python），其日志通过 Python logger 输出，日志名为 `csam.bridge.matlab`，与 Python 应用日志统一管理，输出到 `logs/` 目录。

| 日志来源 | logger 名 | 说明 |
|----------|-----------|------|
| MatlabAdapter | `csam.bridge.matlab` | MATLAB 算法调用、降级、共享会话连接等事件 |
| MatlabEngineProxy | `csam.bridge.matlab` | `matlab.engine` 连接、调用、异常等事件 |

**级别配置**：通过 `CSAM_LOG_LEVEL` 统一控制（与 Python 应用日志相同）。

### 关键事件

以下为系统中需要重点关注的日志事件：

| 事件 | 来源 | 含义 |
|------|------|------|
| `adapter.start` | MatlabAdapter | MatlabAdapter 启动成功 |
| `request.start` | Bridge / MatlabAdapter | 请求处理开始（携带 request_id） |
| `request.crash` | MatlabAdapter | 请求处理过程中发生崩溃 |
| `adapter.stop` | MatlabAdapter | MatlabAdapter 停止 |
| `heartbeat.miss` | Bridge | 心跳丢失 |
| `engine.lost` | Bridge | MATLAB 引擎连接丢失 |
| `fallback.invoke` | MatlabAdapter | MATLAB 不可用，降级到 Python 原型 |

### 日志分析技巧

1. **按 `request_id` 关联请求全链路**：每个请求分配唯一 `request_id`，在 Python 与 MATLAB 日志中均可检索，用于追踪完整调用链。
2. **`error` 级别优先排查**：`error_{date}.log` 仅记录 ERROR 及以上，是定位故障的起点。
3. **结构化日志便于聚合**：`structured_{date}.jsonl` 可导入 ELK / Loki 等日志平台进行聚合分析。
4. **MATLAB 日志与 Python 日志交叉比对**：同一 `request_id` 在两侧均有记录，可定位通信层与算法层的边界问题。

---

## 备份

### 备份对象

| 类别 | 路径 | 说明 |
|------|------|------|
| 配置文件 | `.env` | 环境变量配置（含 HMAC 密钥等敏感信息） |
| 标定数据 | `config/calibration_db.json` | 标定数据库 |
| 材料数据 | `repair_app/core/material_db.json` | 材料数据库 |
| 运行数据 | `morphology_prediction/` | 形貌预测中间文件（点云、速度列表等） |
| 日志 | `logs/` | 应用日志（含 MatlabAdapter 日志） |
| 代码 | git 仓库 | 通过 git 远程仓库备份 |

### 备份策略

| 策略 | 频率 | 保留期 |
|------|------|--------|
| 增量备份 | 每日 | — |
| 完整备份 | 每周 | 4 周 |

> **注意**：`.env` 包含 HMAC 密钥等敏感信息，备份时应加密存储，不应明文提交到 git 仓库。

---

## 恢复

### MATLAB 算法服务崩溃恢复

1. 确认 MATLAB 进程已退出
2. 重启 MATLAB 算法服务：在 MATLAB 中运行 `matlab_bridge_server`
3. GUI 侧重试当前操作，或重新生成修复方案
4. 检查 `logs/app_*.log`（logger 名 `csam.bridge.matlab`）确认崩溃原因

### GUI 崩溃恢复

1. `crash_handler` 自动记录崩溃日志到 `logs/`
2. 重启 GUI：
   ```powershell
   python run_app.py
   ```
3. 检查崩溃日志定位原因

### 数据恢复

- 从 `morphology_prediction/` 目录恢复形貌预测中间文件（`pointlist.npz`、`velocitylist.json` 等）
- 从备份恢复配置文件

### 配置恢复

- 从 `.env.example` 重建 `.env`，按实际环境填写值
- 从备份恢复 `calibration_db.json` 与 `material_db.json`

### 完整重置

当系统状态异常且常规恢复无效时，执行完整重置：

1. 关闭所有进程（GUI + MATLAB）
2. 清理 `logs/` 目录与临时文件
3. 从备份恢复配置文件
4. 重启 MATLAB 算法服务（在 MATLAB 中运行 `matlab_bridge_server`）
5. 重启 GUI（`python run_app.py`）
6. 执行一次完整工作流验证

---

## 升级

### 升级前准备

1. 备份配置和数据（见 [备份](#备份)）
2. 记录当前版本号（`repair_app/__init__.py` 中 `__version__`）
3. 暂存或提交本地改动：
   ```bash
   git stash        # 暂存未提交改动
   # 或
   git commit -am "WIP: 升级前提交"
   ```

### 升级步骤

1. 拉取最新代码：
   ```bash
   git pull origin main
   ```
2. 更新 Python 依赖：
   ```bash
   pip install -r requirements.txt
   ```
3. 更新 MATLAB 算法文件（如算法目录有变更）
4. 重启 MATLAB 算法服务：在 MATLAB 中运行 `matlab_bridge_server`
5. 重启 GUI：
   ```powershell
   python run_app.py
   ```

### 升级后验证

1. 运行测试套件：
   ```bash
   pytest --cov=repair_app
   ```
   覆盖率应 ≥ 60%（CI 门控阈值）。
2. 启动 GUI 验证 MATLAB 算法服务连接正常
3. 执行一次完整工作流：加载模型 → 选区 → 路径生成 → 形貌预测 → 导出

### 回滚

```bash
git checkout <旧版本标签或commit>
pip install -r requirements.txt
# 重启 MATLAB 算法服务（matlab_bridge_server）与 GUI
```

### MATLAB 升级注意事项

- **R2025b 非 ASCII 字符限制**：R2025b 拒绝 `.m` 文件中的非 ASCII 字符，升级 MATLAB 版本前需确认所有算法文件注释为纯 ASCII
- **工具箱许可证变更**：升级后检查工具箱许可证是否仍有效（`license('test', 'Map_Toolbox')` 等），部分算法依赖 `Map_Toolbox`、`Curve_Fitting_Toolbox`

---

## 新增 MATLAB 算法

### 操作步骤

1. **编写算法 `.m` 文件**：将原生算法函数放在 `形貌预测\`（path_planning 算法）或 `路径规划\`（morphology 算法）目录中
   > 注意：磁盘目录名与实际内容相反，参见 [07 开发者文档](07_开发者文档.md) 中的说明。

2. **接入主入口函数**：新算法通过 `run_path_planning.m`（路径规划）或 `run_profile_prediction.m`（形貌预测）调用。如需新增独立算法入口，在 `MatlabAdapter` 中扩展对应的 `_algorithm_fn`。

3. **重启 MATLAB 算法服务**：在 MATLAB 中运行 `matlab_bridge_server`

4. **验证**：启动 GUI 执行完整工作流，检查新算法是否被正确调用

### 关键说明

- **算法 `.m` 文件规范**：
  - 不要使用非 ASCII 字符（R2025b 限制）
  - 不要使用 `clear all` / `fclose all`
  - 不要使用 `evalin('base', 'pack')`
- **降级兼容**：新增 MATLAB 算法时，建议同时在 `LocalEngine` 中提供 Python 原型实现，确保 MATLAB 不可用时可自动降级

---

## 替换算法

### 操作步骤

1. **修改算法 `.m` 文件**：更新原生算法实现
2. **保持入口函数签名不变**：`run_path_planning.m` 或 `run_profile_prediction.m` 的调用签名保持不变，确保向后兼容
3. **递增版本号**（如需）：在算法函数内部或注释中记录版本变更
4. **重启 MATLAB 算法服务**：在 MATLAB 中运行 `matlab_bridge_server`
5. **验证**：调用该算法检查输出是否符合预期

---

## 发布流程

### 发布步骤

1. **代码冻结**：停止新功能合并，仅允许缺陷修复
2. **运行测试**：
   ```bash
   pytest --cov=repair_app
   ```
   覆盖率 ≥ 60%（CI 门控阈值）
3. **更新版本号**：修改 `repair_app/__init__.py` 中的 `__version__`
4. **更新 CHANGELOG**：在 `CHANGELOG.md` 中记录本次变更
5. **打包**：
   ```cmd
   build_windows.bat
   ```
   产物：`dist/CSAM_Repair.exe`
6. **打包验证**：执行 `docs/PACKAGING_GUIDE.md` 中的 7 项检查清单
7. **冒烟测试**：执行完整工作流验证 —— 启动 → 加载模型 → 选区 → 路径生成 → 形貌预测 → 导出
8. **生成发布说明**：创建 `docs/RELEASE_NOTES_V1.0.md`
9. **打标签**：
   ```bash
   git tag v1.x.0
   git push origin v1.x.0
   ```
10. **分发**：
    - Windows：直接分发 `dist/CSAM_Repair.exe`
    - macOS：需代码签名 + 公证后分发

---

## 环境变量运维参考

下表列出所有运维相关环境变量及其作用。默认值标注于括号内。

### Bridge 通信层

| 环境变量 | 默认值 | 作用 |
|----------|--------|------|
| `CSAM_BRIDGE_TIMEOUT_MS` | `30000` | 请求超时（毫秒），复杂算法可调大 |
| `CSAM_BRIDGE_HEARTBEAT_MS` | `5000` | Bridge 心跳间隔（毫秒） |
| `CSAM_BRIDGE_MAX_RETRIES` | `3` | 请求最大重试次数 |

### 通用

| 环境变量 | 默认值 | 作用 |
|----------|--------|------|
| `CSAM_LOG_LEVEL` | `INFO` | Python 应用日志级别（DEBUG/INFO/WARNING/ERROR），同时控制 MatlabAdapter 日志 |
| `CSAM_HMAC_SECRET` | （开发内置密钥） | HMAC 密钥，生产环境必须设置 |
| `CSAM_ALGORITHM_ENGINE` | `auto` | 算法引擎选择（`auto` / `matlab` / `python`），`auto` 优先 MATLAB 不可用降级 Python |
| `CSAM_ZMQ_ADDRESS` | 平台相关 | ZMQ 连接地址（macOS/Linux 默认 `ipc:///tmp/csam_engine`，Windows 默认 `tcp://127.0.0.1:5555`） |

> 完整环境变量模板参见项目根 `.env.example`。

---

## 日常运维检查清单

建议每日执行以下检查，确保系统正常运行：

- [ ] MATLAB 进程存活（`matlab_bridge_server` 运行中）
- [ ] 端口 5555（ZMQ）可访问
- [ ] 日志无 `ERROR` 级别条目
- [ ] 磁盘空间充足（`logs/` 与 `morphology_prediction/` 目录）
- [ ] MATLAB 内存不超过阈值（默认 4096 MB）
- [ ] 工具箱许可证有效（`license('test', 'Map_Toolbox')` 返回 `1`）
- [ ] 备份已完成

### 检查命令参考

```powershell
# 检查 MATLAB 进程
Get-Process -Name MATLAB -ErrorAction SilentlyContinue

# 检查端口可访问性
Test-NetConnection -ComputerName 127.0.0.1 -Port 5555
```

```matlab
% 在 MATLAB 中检查工具箱许可证
license('test', 'Map_Toolbox')          % 返回 1 表示可用
license('test', 'Curve_Fitting_Toolbox')
```

---

## 性能调优

### 关键调优参数

| 调优方向 | 环境变量 | 默认值 | 调优建议 |
|----------|----------|--------|----------|
| 请求超时 | `CSAM_BRIDGE_TIMEOUT_MS` | 30000（30 秒） | 复杂算法（如 `profile_predict`、`generate_path`）耗时较长，可调大至 120000 |
| 心跳间隔 | `CSAM_BRIDGE_HEARTBEAT_MS` | 5000 | 网络不稳定时可调小以更快检测断连 |
| 日志级别 | `CSAM_LOG_LEVEL` | INFO | 生产用 `INFO`，排查问题时用 `DEBUG`（注意 DEBUG 日志量大），同时控制 MatlabAdapter 日志 |
| 算法引擎 | `CSAM_ALGORITHM_ENGINE` | `auto` | `auto` 优先 MATLAB，不可用降级 Python；`python` 强制使用原型算法 |

### 调优原则

1. **生产环境用 `INFO` 级别**：`DEBUG` 日志量大幅增加，影响性能与磁盘占用，仅在排查问题时临时启用。
2. **超时与算法复杂度匹配**：`generate_path`（120000 ms 超时）、`profile_predict`（120000 ms）等复杂算法需要更长的请求超时，确保 `CSAM_BRIDGE_TIMEOUT_MS` 大于算法超时。
3. **心跳与重连配合**：心跳丢失阈值触发重连，网络不稳定环境可适当调大丢失阈值避免频繁重连。
4. **降级策略选择**：生产环境建议使用 `auto` 模式，保证 MATLAB 故障时业务连续性；调试 Python 原型时可用 `python` 模式。

---

## 相关文档

- [02 用户安装指南](02_用户安装指南.md)
- [06 故障排查](06_故障排查.md)
- [07 开发者文档](07_开发者文档.md)
- [09 API 文档](09_API文档.md)
- [10 架构文档](10_架构文档.md)
- `docs/PACKAGING_GUIDE.md`：打包指南（7 项检查清单）
- `docs/MATLAB_INTEGRATION.md`：MATLAB 集成指南
- `docs/archive/`：历史文档归档目录
- 项目根 `.env.example`：环境变量配置模板

---

*本文档由技术写作团队维护，最后更新于 2026-07-10。*
